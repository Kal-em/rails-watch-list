<h1>list all of the movies</h1>

<ul class="list-group">
  <% @list.movies.each do |movie| %>
    <li class="list-group-item"> <%= movie.title %></li>
  <% end %>
</ul>

<%= link_to 'add a new movie to the list', new_list_bookmark_path(@list) %>
