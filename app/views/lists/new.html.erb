<div class="form">
  <h1>Create a new movie list</h1>

  <div class="form-size">
    <%= simple_form_for(@list) do |f| %>
      <%= f.input :name %>
      <%= f.submit class: 'btn btn-primary'%>
    <% end %>
  </div>
</div>
